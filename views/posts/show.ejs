<!DOCTYPE html>
<html>

<head>
  <% include ../partials/head %>
    <link rel="stylesheet" href="/css/post_show.css" type="text/css">
</head>

<body>
  <% include ../partials/nav %>

    <div class="container post post-show">
      <div class="buttons">
        <a class="btn btn-default" href="/posts">Back</a>
        <% if(isAuthenticated && post.author && currentUser.id == post.author.id){ %>
        <a class="btn btn-default" href="/posts/<%= post._id %>/edit">Edit</a>
        <a class="btn btn-default" href="/posts/<%= post._id %>?_method=delete" onclick="confirm('Do you want to delete this?')?this.parentElement.submit():null;">Delete</a>
        <% } %>
      </div>

      <div class="contentBox">
        <h3 class="contentBoxTop"><%= post.title %></h3>
        <div class="row">
            <div class="col s12">
              <div class="card">
                <div class="card-content">
                  <span class="card-title"><%= post.author ? post.author.username : "" %> ( <%= post.author.name %>)</span>
                  <p><%= post.body %></p>
                </div>
                <div class="card-action">
                  <a class="white-text"> 
                    작성 시간 :                
                    <%= post.createdDate %>
                    <%= post.createdTime %> 
                    <br>
                  </a>
                  <a class="white-text" href="/users/<%= post.author.username %>">
                    작성자 컨택 : <%= post.author.name %>
                  </a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>

</html>
